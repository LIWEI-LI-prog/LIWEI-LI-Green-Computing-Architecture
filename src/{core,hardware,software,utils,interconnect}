# 1. 源代码目录
mkdir -p src/{core,hardware,software,utils,interconnect}

# 2. 示例代码
mkdir -p examples/{basic,advanced,tutorials,demo}

# 3. 测试目录
mkdir -p tests/{unit,integration,benchmarks}

# 4. 工具脚本
mkdir -p scripts/{setup,build,analysis,deploy}

# 5. 配置文件
mkdir -p config/{ci,dev,prod}

# 6. 文档补充
mkdir -p docs/{api,architecture,guides,benchmarks}